# Experiment 05: Recognize Valid Control Structures Syntax Using YACC

## Aim
To write a program using LEX and YACC to recognize valid control structure syntax of C language (such as `for` loop, `while` loop, `if-else`, `if-else-if`, `switch-case`, etc.).
